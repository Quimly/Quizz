{% extends 'base.html.twig' %}

{% block javascripts %}
    <script src="{{ asset('jquery.min.js') }}"></script>
    <script src="{{ asset('build/asset/prototype.js') }}"></script>
{% endblock %}

{% block title %}Hello{% endblock %}

{% block body %}
<p>Ajouter une question!</P>


    {#<div class="form_wrap">#}

        {{ form_start(form, {'attr': {'class': 'form_main', 'autocomplete': 'off' }}) }}

        {#{{ form_row(form.entitled, {'label': 'Intitulé'}) }}#}

        {#{{ form_row(form.image.file, {'label': 'Image'}) }}#}

        {#{{ form_row(form.answer.image.alt, {'label': 'Description de l\'image'}) }}#}

        {#<ul id="formAnswer" class="answers" data-prototype="{{ form_widget(form.answers.vars.prototype)|e('html_attr') }}">#}
            {#{% for answer in form.answers %}#}
                {#<li>{{ form_row(answer.entitled) }}#}
                	{#{{ form_row(answer.correct) }}#}
                	{#{{ form_row(answer.explanation) }}#}
                	{#{{ form_row(answer.image.file) }}#}
                	{#{{ form_row(answer.image.alt) }}#}
                {#</li>#}
            {#{% endfor %}#}
    	{#</ul>#}
        {##}
        {##}

        {#<button type="submit">Créer!</button>#}

        {#{{ form_end(form) }}#}

    {#</div>#}


    {{ form_start(form) }}
    {# ... #}

    {{ form_row(form.entitled, {'label': 'Intitulé'}) }}

    {{ form_row(form.image.file, {'label': 'Image'}) }}


    <a href="#"
       class="add-another-collection-widget"
       data-list="#answer-fields-list">Ajouter une réponse</a>

    {# store the prototype on the data-prototype attribute #}
    <ul id="answer-fields-list"
        data-prototype="{{ form_widget(form.answers.vars.prototype)|e }}"
        data-widget-tags="{{ '<li></li>'|e }}">
        {% for answerField in form.answers %}
            <li>
                {{ form_errors(answerField) }}
                {{ form_widget(answerField) }}
            </li>
        {% endfor %}
    </ul>



    <button type="submit">Créer!</button>
    {{ form_end(form) }}

{% endblock %}
